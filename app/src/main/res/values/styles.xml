<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>

void DlgAlarmInfoInterface::ShowDlgAlarmInfoInterface(BOOL bShow, CWnd* pParent)
{
	static DlgAlarmInfoInterface* s_pDlgAlarmInfo = NULL;
	if (!s_pDlgAlarmInfo)
	{
		s_pDlgAlarmInfo = new DlgAlarmInfoInterface(pParent);
		s_pDlgAlarmInfo->Create(DlgAlarmInfoInterface::IDD, pParent);
	}
	s_pDlgAlarmInfo->ShowWindow(bShow?SW_SHOW:SW_HIDE);
}
